# Enable display
CONFIG_ZMK_DISPLAY=y

# Enable Bongo Cat specifically
CONFIG_ZMK_WIDGET_BONGO_CAT=y

# Ensure WPM tracking is enabled (needed for animation)
CONFIG_ZMK_WPM=y

# LVGL settings for animation
CONFIG_LV_USE_ANIMATION=y
CONFIG_LV_USE_IMG=y